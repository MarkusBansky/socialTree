#include "scenegraph.h"
#include "corehandler.h"

SceneGraph::SceneGraph()
{
    //for (int i = 0; i < coreHandler.getTree().root.nodes.size(); i++)
    //{
    //    node* Node = coreHandler.getTree().root.nodes[i];
    //}
}


